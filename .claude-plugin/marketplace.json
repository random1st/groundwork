{
  "name": "groundwork",
  "owner": {
    "name": "Roman Voronin",
    "email": "voroninr@gmail.com"
  },
  "description": "Reasoning discipline for AI agents — cross-model audit, cross-CLI routing, a reasoning baseline that builds its own modular FPF corpus from upstream, and an evidence-before-claim verification gate. Four plugins that turn a single AI session into a calibrated one.",
  "plugins": [
    {
      "name": "tribunal",
      "source": "./plugins/tribunal",
      "description": "Independent code review by two other AI CLIs. Whichever CLI you are running stays as the arbiter; the two others (any combo of Claude, Codex, Gemini) audit the code in parallel and you read both verdicts before deciding. Use for pre-merge reviews, security-critical code, and changes you don't want to ship alone."
    },
    {
      "name": "delegate",
      "source": "./plugins/delegate",
      "description": "Routing matrix for sending work to another AI CLI. Tells you which model and which reasoning effort to use for each task — code review, architecture analysis, whole-project scans, quick documentation. Uses raw codex exec and gemini binaries; no wrapper required."
    },
    {
      "name": "fpf",
      "source": "./plugins/fpf",
      "description": "Reasoning baseline plus a local builder for the full FPF corpus. The SKILL.md ships the always-loaded operational subset (ADI cycle, calibration tags, I/D/S, weakest-link). A bundled refresh script pulls the upstream ailev/FPF spec and splits it into navigable modules, cards, and a relation graph on first use — and silently no-ops when upstream hasn't moved. No FPF text is bundled with the plugin."
    },
    {
      "name": "verify",
      "source": "./plugins/verify",
      "description": "Refuses to claim completion without proof. Identifies the command that would prove the claim, runs it fresh, reads the actual output, and reports PASS or FAIL with the evidence. Companion to the tag discipline in fpf — verification is what makes [OBSERVED] tags trustworthy."
    }
  ]
}
